openapi: "3.0.1"
info:
  version: 0.0.1
  title: Woodnet
  description: Woodnet Backend API
paths:
  /test:
    get:
      produces:
      - application/json
      operationId: getTest
      x-amazon-apigateway-integration:
        httpMethod: POST
        uri: ${test_lambda_invoke_arn}
        type: aws_proxy
  /toggle-ark:
    get:
      produces:
      - application/json
      operationId: getTest
      x-amazon-apigateway-integration:
        httpMethod: POST
        uri: ${toggle_ark_lambda_invoke_arn}
        type: aws_proxy
definitions:
  Input:
    type: object
    required:
    - a
    - b
    - op
    properties:
      a:
        type: number
      b:
        type: number
      op:
        type: string
        description: binary op of ['+', 'add', '-', 'sub', '*', 'mul', '%2F', 'div']
    title: Input
  Output:
    type: object
    properties:
      c:
        type: number
    title: Output
  Result:
    type: object
    properties:
      input:
        "$ref": "#/definitions/Input"
      output:
        "$ref": "#/definitions/Output"
    title: Result